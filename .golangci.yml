version: "2"

linters:
  settings:
    errcheck:
      # http.Response.Body.Close() is universally ignored in Go HTTP code.
      exclude-functions:
        - (io.ReadCloser).Close

issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - errcheck
